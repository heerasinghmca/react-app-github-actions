name: checkout
on:
  push:
    branches:
      - master
      - develop
jobs:
  checkout-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: develop
